# Environment Setup

## Quick Start
1. Install dependencies: `npm install`
2. Configure `.env`
3. Start app

## Required Env Vars
- `SUPABASE_URL`
- `SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`

## Verify
- App starts without errors
- Core flow works
